project-folder/
├── main.tf          # Main entry point for Terraform configurations
├── variables.tf     # Define all input variables
├── outputs.tf       # Define outputs for key resource details
├── provider.tf      # AWS provider configuration
├── ecs/
│   ├── cluster.tf   # ECS cluster definition
│   ├── service.tf   # ECS service and task definitions
│   ├── iam.tf       # IAM roles for ECS
├── alb/
│   ├── alb.tf       # Application Load Balancer configuration
│   ├── target.tf    # ALB target groups and listener rules
├── api_gateway/
│   ├── api_gateway.tf  # API Gateway configuration
├── vpc/
│   ├── vpc.tf       # VPC, subnets, and route tables
│   ├── security.tf  # Security groups for ECS and ALB

